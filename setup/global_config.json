{
    "global_settings": {
        "_comment": "interval is int in seconds",
        "config": {
            "ip_address": "10.0.0.1/24",
            "default_gateway": "10.0.0.1"
        },
        "monitors_configuration": {
            "interval": 60,
            "ftt": 3
            
        },
        "credentials": {
            "default username": "admin",
            "default password": "12345"
        },
        "alerting": {
            "email" : "admin@lab.local",
            "email_enabled": "true",
        },
        "logging": {
            "target": "local DB or Syslog server"
        }
}